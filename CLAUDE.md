@AGENTS.md

*NEVER* use an em dash '—'. it communicates 'AI' and not human touch.

*NEVER* create a git branch unless explicitly requested. Commit directly to the
current branch (usually main). Only branch when the user asks for it.