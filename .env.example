# The chat at /chat talks to Claude through the Vercel AI Gateway.
# Get a key (includes $5/mo free) at https://vercel.com/d?to=/[team]/~/ai/api-keys
# then copy this file to .env.local and fill it in:
AI_GATEWAY_API_KEY=
